import styled from "styled-components";

export const FixedText = styled.div`
  color: white;
  font-size: 20px;
  font-weight: 600;
  position: fixed;
  left: 50%;
  top: 50%;
  transform: translate(-50%, -50%);
`;
